[package]
name = "dist-typestate" # Distributed Typestate
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0" 
authors = ["Yamato Saito"]

[dependencies]
rusqlite = { version = "0.35", features = ["bundled"] }
distributed-typestate-macro = { path = "../distributed-typestate-macro" }
